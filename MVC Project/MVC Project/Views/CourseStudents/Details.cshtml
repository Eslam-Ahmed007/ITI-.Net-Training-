@model MVC_Project.Models.CourseStudents

@{
    ViewData["Title"] = "Course Students Details";
}

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.css" rel="stylesheet">

<style>
    body {
        background-color: #f8f9fa;
        color: #333;
    }

    .card {
        margin-top: 20px;
    }

    .animate__zoomIn {
        animation-duration: 1s;
    }
</style>

<h1 class="text-center mb-4 animate__animated animate__fadeIn">Course Students Details</h1>

<div class="row justify-content-center">
    <div class="col-md-6">
        <div class="card animate__animated animate__zoomIn">
            <div class="card-body">
                <h4 class="card-title">Course Students</h4>
                <hr />
                <dl class="row">
                    <dt class="col-sm-4">Degree</dt>
                    <dd class="col-sm-8">@Model.Degree</dd>
                    <dt class="col-sm-4">Course</dt>
                    <dd class="col-sm-8">@Model.Course?.Name</dd>
                    <dt class="col-sm-4">Student</dt>
                    <dd class="col-sm-8">@Model.Student?.Name</dd>
                </dl>
                <a asp-action="Index" class="btn btn-primary">Back to List</a>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>